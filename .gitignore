/target
**/*.rs.bk

.env

# Console build + tooling artifacts
console/node_modules/
console/dist/
console/test-results/
console/playwright-report/
console/tsconfig.tsbuildinfo

# Local gateway state from `make run`
/.rapidrouter
# The pre-rename data dir, so an older checkout does not commit one.
/.rapid-data

# macOS finder droppings
.DS_Store

# Working notes and generated artifacts, not part of the project
/.context/
